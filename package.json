{
  "dependencies": {
    "@pinecone-database/pinecone": "^2.0.0",  // Pinecone client
    "axios": "^0.21.1",  // Axios for HTTP requests
    "next": "^12.0.0",  // Ensure Next.js version is correct
    "dotenv": "^10.0.0"  // For local environment variables (if needed)
  }
}
