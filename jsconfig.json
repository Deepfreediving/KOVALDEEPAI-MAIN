{
  "compilerOptions": {
    "baseUrl": ".",  // Base directory for resolving modules
    "paths": {
      "@lib/*": ["lib/*"],  // Correctly map the alias to the lib directory
      "@components/*": ["pages/components/*"],
      "@pages/*": ["pages/pages/*"],
      "@styles/*": ["pages/styles/*"],
      "@/*": ["pages/*"]
    },
    "jsx": "react-jsx"  // Optional, if using JSX with React
  },
  "include": [
    "pages/**/*",  // Include pages
    "components/**/*",  // Include components
    "next.config.mjs",
    "vercel.json"
  ],
  "exclude": [
    "node_modules",
    ".next"
  ]
}
